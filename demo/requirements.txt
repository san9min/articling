# articling 데모 서버 전용 의존성 — 패키지 본체(pyproject.toml)와는 별개.
# articling 자체 의존성(pydantic/openpyxl/pymupdf 등)은 별도로 설치돼 있어야 한다
# (repo 루트에서 `pip install -e .`).
fastapi>=0.110
uvicorn>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0

# CAPTION_OF/REFERENCES 제안(선택, 프런트 체크박스로 opt-in)에 필요 —
# articling 본체의 [relations] extra와 동일
openai>=1.40
